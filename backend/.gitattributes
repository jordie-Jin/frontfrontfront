# 1. 모든 텍스트 파일은 항상 LF로 고정 (Windows에서도 LF 사용 권장)
* text eol=lf

# 2. 실행 스크립트는 항상 LF로 고정 (Unix/Linux/macOS 호환)
/gradlew text eol=lf
*.sh text eol=lf

# 3. 윈도우 배치 파일은 항상 CRLF로 고정
*.bat text eol=crlf
*.cmd text eol=crlf

# 4. 바이너리 파일은 변환하지 않음
*.jar binary
*.png binary
*.jpg binary
